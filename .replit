run = "uvicorn server:app --host 0.0.0.0 --port 8000"

[[ports]]
localPort = 8000
externalPort = 80

[deployment]
run = ["sh", "-c", "uvicorn server:app --host 0.0.0.0 --port $PORT"]
deploymentTarget = "cloudrun"